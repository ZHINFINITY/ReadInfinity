/**
 * Read∞ is distributed as a direct-device reader in this fork.
 * Keep cloud/account integrations compiled for upstream compatibility, but make
 * their runtime entry points unavailable in this product build.
 */
export const IS_OFFLINE_BUILD = true;
